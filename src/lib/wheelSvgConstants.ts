/** Single source of truth for wheel SVG geometry (user units). */
export const WHEEL_RADIUS = 154
export const WHEEL_VIEWBOX_PAD = 8
/** viewBox width/height: must match EmotionWheel `<svg viewBox>`. */
export const WHEEL_VIEWBOX_SIDE = 2 * (WHEEL_RADIUS + WHEEL_VIEWBOX_PAD)
